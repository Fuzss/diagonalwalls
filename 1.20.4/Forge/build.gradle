apply from: "https://raw.githubusercontent.com/Fuzss/modresources/main/gradle/v2/forge.gradle"

dependencies {
    // Puzzles Lib
    modApi libs.puzzleslib.forge

    // Diagonal Blocks
    modApi(include(libs.diagonalblocks.forge.get())) {
        exclude group: "fuzs.puzzleslib"
    }
}
